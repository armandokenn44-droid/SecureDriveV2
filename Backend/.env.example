# Copy this file to ".env" (same folder) and fill in your REAL values.
# .env is gitignored — .env.example is the only one that should ever be committed.

PORT=4000

# CORS: the origin(s) allowed to call this backend. Your Vite dev server default.
FRONTEND_ORIGIN=http://localhost:5173

# --- Database (Neon PostgreSQL) ---
# Paste the full connection string Neon gave you (Connect -> Connection string).
# Keep the "?sslmode=require" at the end — Neon requires SSL.
DATABASE_URL=postgresql://neondb_owner:tstrdwj%40gtydsy.00yt@ep-small-mountain-ayssfx77-pooler.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
# --- Auth (used once we build login) ---
# Any long random string. You can generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=b8d069976d1a72da30195773402945f66c3ac65148f5a432c7b44741bc0b651295bfe73d4148ef254ab059e152b019c8
# --- Amazon S3 ---
# Get these from AWS IAM (create a dedicated IAM user with S3-only permissions,
# scoped to this one bucket — never use your AWS root account keys).
AWS_REGION=eu-west-2
AWS_ACCESS_KEY_ID=AKIASMGDV7KE5ALADNF4
AWS_SECRET_ACCESS_KEY=wIOn1nqglKzlzpnA86ztd8eK/+k+0dx4pEcp9OHE
AWS_S3_BUCKET=drivetentee

